{
  "args": ["https://moonvy.com/project/<PROJECT_ID>/<DIR_ID>", "--limit", "3"],
  "expect": {
    "rowCount": { "min": 1, "max": 3 },
    "columns": ["id", "name", "type", "parentId", "projectId", "url"],
    "types": {
      "id": "string",
      "name": "string",
      "type": "string",
      "parentId": "string",
      "projectId": "string",
      "url": "string"
    },
    "notEmpty": ["id", "url"]
  }
}
